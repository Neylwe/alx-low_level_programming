#include <stdio.h>

/**
 * main - writes a function-like macro ABS (x) that computes the absolute
 * value of a number of x
 * Return: void
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
