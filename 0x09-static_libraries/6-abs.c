#include "main.h"
/**
 * _abs - Function that computes the absolute value of an integer
 *
 * @n: an integer output
 * Return: absolute value of number n
 *
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}
